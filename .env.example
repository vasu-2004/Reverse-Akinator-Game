# ============================================================
# Reverse Akinator — Environment Variables
# ============================================================
# Copy this file to .env and fill in the values for your provider.
#   cp .env.example .env        (macOS / Linux)
#   copy .env.example .env      (Windows)
# ============================================================

# ---- Server ----
PORT=3000

# ---- LLM Provider ----
# Supported values: openai | google_genai | anthropic
DEFAULT_LLM_PROVIDER=google_genai

# ---- Model name ----
MODEL_NAME=gemini-2.0-flash

# ---- Supported providers (JSON array) ----
SUPPORTED_LLM_PROVIDERS=["google_genai"]

# ============================================================
# Google GenAI / Vertex AI  (3 auth modes — pick ONE)
# ============================================================
#
# MODE 1 — GCP Application Default Credentials (ADC)
#   Best for: running on GCP (GCE/GKE/Cloud Run) or locally
#   after running gcloud auth application-default login.
#   Only MODEL_PROJECT_ID is required; no keys or JSON needed.
#
MODEL_PROJECT_ID=your-gcp-project-id
MODEL_LOCATION=us-central1
#
# MODE 2 — Explicit service account JSON
#   Uncomment and paste the JSON if you cannot use ADC.
#
# MODEL_SERVICE_ACCOUNT_JSON='{ ... }'
#
# MODE 3 — Google API key (simplest, not Vertex AI)
#
# GOOGLE_API_KEY=your-google-api-key-here

# ============================================================
# OpenAI  (if DEFAULT_LLM_PROVIDER=openai)
# ============================================================
# OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_BASE_URL=https://api.openai.com/v1

# ============================================================
# Anthropic  (if DEFAULT_LLM_PROVIDER=anthropic)
# ============================================================
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# ---- Logging ----
# Set to DEBUG to see provider/model info per request
LOG_LEVEL=INFO
